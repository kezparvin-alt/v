# Brew Water Calculator V1.8 — phone PWA

Upload the `site` folder to an HTTPS static host. Open its URL on Android Chrome and select **Install app** / **Add to Home screen**.

This build has the phone workflow and mineral treatment engine connected. It remains a field-test build: exact workbook parity for the full sparge speciation and mash-pH branches still requires numerical regression against representative workbook outputs.
